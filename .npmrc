# Reject installing any package version younger than 3 days (4320 minutes).
# Defense against supply-chain attacks via newly-published malicious versions.
# Picked up by both npm (>=11.5) and pnpm (>=10).
# See: https://docs.npmjs.com/cli/v11/using-npm/config#minimum-release-age
minimum-release-age=4320
